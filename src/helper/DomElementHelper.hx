package helper;

//import helper.DomElementHelperBuilder;//for whatever reason this class won't work if this import is included

/**
 * ...
 * @author Chris Anderson
 */
@:build(helper.DomElementHelperBuilder.build("bin/"))
class DomElementHelper
{
}